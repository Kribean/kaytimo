
export const metadata = {
  title: "Activités Martinique",
  description: "Les activités aux trois ilets",
};

export default function CardActivitiesLayout({ children }) {
  return (

<section>{children}</section>

  );
}
